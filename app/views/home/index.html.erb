<% @posts.each do |post| %>
<div class="post">
	<h2 class="title"><%= link_to post.title, post %></h2>
	<p><%= truncate(post.body, length: 100) %></p>
	<p class="note">
		訪客回應：<%= post.comments.count %> ｜ 
		<%= link_to "繼續閱讀..", post %>
	</p>
</div>
<% end %>